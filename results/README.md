# Reproducibility results

This directory will hold the **approved expected outputs** associated with reproducibility releases.

Intended contents include:

- machine-readable metric summaries;
- thesis tables;
- thesis figures;
- artifact manifests;
- compact reference outputs needed for regression checks.

Arbitrary development runs should not be committed here. Approved result artifacts must be traceable to a source commit and frozen input/configuration hashes.
