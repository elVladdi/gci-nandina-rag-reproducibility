# Experiments

This directory will hold manifests for frozen experimental reference presets.

Each reference experiment should identify:

- source repository commit;
- configuration hash;
- dataset/corpus hashes;
- environment/dependency identity;
- expected result registry;
- output hashes or tolerances.

Custom user runs should write to their own result directories rather than modifying frozen reference manifests.
