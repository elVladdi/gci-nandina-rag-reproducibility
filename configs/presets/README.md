# Reference presets

This directory will contain frozen configurations for identifiable experimental reference releases.

A preset should capture dataset/corpus versions, split policy, retrieval parameters, enabled stages and expected manifest identifiers. Reference presets may enforce exact SHA-256 hashes.

Do not place ad hoc tuning configurations here.
